var shark = function () {
  function chunk() {

  }
  function compact(arr) {
    if (!array) {
      return []
    }
    var res = []
    for (var i = 0; i < arr.length; i++) {
      if (arr[i]) {
        res.push(arr[i])
      }
    }
    return res
  }
  return {
    chunk: chunk,
    compact: compact,
  }







}